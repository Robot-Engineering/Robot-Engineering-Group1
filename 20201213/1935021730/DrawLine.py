import turtle
class Drawline:
    def line(start,end,size,color):
        turtle.color(color)
        turtle.pensize(size)
        turtle.penup()
        turtle.goto(start)
        turtle.pendown()
        turtle.goto(end)
        turtle.penup()
        
    
    
    
        
